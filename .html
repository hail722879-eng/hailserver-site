<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HailServer — Анархия и Выживание</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Press+Start+2P&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css"/>
    
    <style>
        :root {
            --bg-dark: #0a0a0c;
            --bg-panel: #121217;
            --accent-purple: #7c3aed;
            --accent-blue: #3b82f6;
            --text-main: #ffffff;
            --text-muted: #8e99ab;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            background-color: var(--bg-dark);
            color: var(--text-main);
            font-family: 'Inter', sans-serif;
            line-height: 1.6;
            overflow-x: hidden;
        }

        .bg-glow {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: radial-gradient(circle at 50% -10%, #1e103d 0%, transparent 60%);
            z-index: -1;
        }

        header {
            padding: 100px 20px;
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .hero-content { z-index: 5; max-width: 700px; }

        .logo-text {
            font-family: 'Press Start 2P', cursive;
            font-size: clamp(24px, 5vw, 46px);
            text-shadow: 4px 4px 0px var(--accent-purple);
            margin-bottom: 25px;
        }

        .hero-desc { color: var(--text-muted); font-size: 17px; margin-bottom: 35px; }

        /* IP с плавным RGB переливом */
        .ip-badge {
            display: inline-flex;
            align-items: center;
            gap: 12px;
            color: #fff;
            font-family: 'Press Start 2P', cursive;
            font-size: 13px;
            padding: 18px 25px;
            cursor: pointer;
            border-radius: 10px;
            position: relative;
            background: #0a0a0c;
            transition: 0.3s transform;
            z-index: 1;
        }

        .ip-badge::before {
            content: '';
            position: absolute;
            inset: -2px;
            z-index: -1;
            background: linear-gradient(90deg, #7c3aed, #3b82f6, #9333ea, #7c3aed);
            background-size: 300% 100%;
            border-radius: 12px;
            animation: moveGradient 4s linear infinite;
        }

        @keyframes moveGradient {
            0% { background-position: 0% 50%; }
            100% { background-position: 100% 50%; }
        }

        .ip-badge:active { transform: scale(0.96); }

        .btn-group { display: flex; gap: 15px; flex-wrap: wrap; margin-top: 20px; }

        .btn {
            padding: 18px 30px;
            border-radius: 10px;
            text-decoration: none;
            font-family: 'Press Start 2P', cursive;
            font-size: 10px;
            color: white;
            transition: 0.2s;
            cursor: pointer;
            border: none;
        }

        .btn-purple { background: var(--accent-purple); box-shadow: 0 4px 15px rgba(124, 58, 237, 0.3); }
        .btn-outline { background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.1); }
        .btn:hover { transform: translateY(-3px); filter: brightness(1.2); }

        /* Визуал справа */
        .hero-visual {
            position: relative; width: 350px; height: 350px;
            display: flex; justify-content: center; align-items: center;
        }

        .crossed-items {
            position: relative; font-size: 100px;
            animation: floating 3s ease-in-out infinite;
            filter: drop-shadow(0 0 15px var(--accent-purple));
        }

        @keyframes floating {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        .item-sword { position: absolute; transform: translate(-50%, -50%) rotate(-45deg); color: #fff; }
        .item-pickaxe { position: absolute; transform: translate(-50%, -50%) rotate(45deg); color: #ccc; }

        /* Сетка инфо-блоков */
        .container { max-width: 1200px; margin: 60px auto; padding: 0 20px; }
        
        .grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 25px; }

        .feature-card {
            background: var(--bg-panel);
            border-radius: 15px; padding: 40px;
            display: flex; align-items: flex-start; gap: 25px;
            border: 1px solid rgba(255,255,255,0.03);
            transition: 0.3s;
        }

        .feature-card:hover { border-color: var(--accent-purple); transform: translateY(-5px); }

        .card-icon { width: 64px; height: 64px; image-rendering: pixelated; flex-shrink: 0; }

        .card-info h3 { font-family: 'Press Start 2P', cursive; font-size: 14px; margin-bottom: 12px; color: #fff; }
        .card-info p { color: var(--text-muted); font-size: 14px; }

        @media (max-width: 992px) {
            header { flex-direction: column; text-align: center; padding: 50px 20px; }
            .hero-visual { display: none; }
            .grid { grid-template-columns: 1fr; }
        }

        footer { padding: 40px; text-align: center; color: #444; font-size: 10px; font-family: 'Press Start 2P'; border-top: 1px solid #16161a; }
    </style>
</head>
<body>

    <div class="bg-glow"></div>

    <header>
        <div class="hero-content">
            <div class="logo-text">HAILSERVER</div>
            <p class="hero-desc">Классическая анархия и ванильное выживание. Сервер доступен для игры с версий <b>1.16.5 по 1.21</b>. Заходи и начни свою историю в свободном мире.</p>
            
            <div class="ip-badge clickable" id="copyIp">
                <i class="fas fa-terminal"></i>
                <span>HAILSERV.DDNS.NET:28299</span>
            </div>

            <div class="btn-group">
                <a href="https://t.me/+nc-Z7hww_Ic3Zjdk" target="_blank" class="btn btn-purple clickable">TELEGRAM</a>
                <a href="https://discord.gg/NGH5gu3ayn" target="_blank" class="btn btn-outline clickable">DISCORD</a>
            </div>
        </div>
        
        <div class="hero-visual">
            <div class="crossed-items">
                <i class="fas fa-sword item-sword"></i>
                <i class="fas fa-pickaxe item-pickaxe"></i>
            </div>
        </div>
    </header>

    <div class="container">
        <div class="grid">
            <div class="feature-card">
                <img src="https://assets.mcasset.cloud/1.20.1/assets/minecraft/textures/block/grass_block_side.png" class="card-icon">
                <div class="card-info">
                    <h3>РЕЖИМ АНАРХИИ</h3>
                    <p>Полное отсутствие приватов территории. Гриферство, кражи и создание альянсов — всё это является основой нашего геймплея.</p>
                </div>
            </div>

            <div class="feature-card">
                <img src="https://assets.mcasset.cloud/1.20.1/assets/minecraft/textures/item/spyglass.png" class="card-icon">
                <div class="card-info">
                    <h3>ВЕРСИИ 1.16.5 — 1.21</h3>
                    <p>Играй с любой удобной тебе версии. Мы поддерживаем кросс-версионный вход, чтобы объединить всё сообщество.</p>
                </div>
            </div>

            <div class="feature-card">
                <img src="https://assets.mcasset.cloud/1.20.1/assets/minecraft/textures/item/diamond_sword.png" class="card-icon">
                <div class="card-info">
                    <h3>PVP БЕЗ ГРАНИЦ</h3>
                    <p>Классическая боевая система без искусственных ограничений. Испытайте свои навыки в честных или не очень честных битвах.</p>
                </div>
            </div>

            <div class="feature-card">
                <img src="https://assets.mcasset.cloud/1.20.1/assets/minecraft/textures/block/repeater.png" class="card-icon">
                <div class="card-info">
                    <h3>ТЕХНИЧЕСКИЙ TPS</h3>
                    <p>Сервер оптимизирован для работы на версии 1.21. Мы минимизировали задержки, чтобы ПВП-сражения были максимально комфортными.</p>
                </div>
            </div>
        </div>
    </div>

    <footer>
        © 2026 HAILSERVER — ВАНИЛЬНОЕ ВЫЖИВАНИЕ (1.16.5 - 1.21)
    </footer>

    <script>
        function playClick() {
            const context = new (window.AudioContext || window.webkitAudioContext)();
            const osc = context.createOscillator();
            const gain = context.createGain();
            osc.type = 'sine';
            osc.frequency.setValueAtTime(150, context.currentTime);
            osc.frequency.exponentialRampToValueAtTime(0.01, context.currentTime + 0.1);
            gain.gain.setValueAtTime(0.1, context.currentTime);
            gain.gain.exponentialRampToValueAtTime(0.01, context.currentTime + 0.1);
            osc.connect(gain);
            gain.connect(context.destination);
            osc.start();
            osc.stop(context.currentTime + 0.1);
        }

        document.querySelectorAll('.clickable').forEach(btn => {
            btn.addEventListener('click', playClick);
        });

        document.getElementById('copyIp').addEventListener('click', function() {
            const ip = "hailserv.ddns.net:28299";
            navigator.clipboard.writeText(ip);
            const span = this.querySelector('span');
            span.innerText = "IP СКОПИРОВАН";
            setTimeout(() => { span.innerText = "HAILSERV.DDNS.NET:28299"; }, 2000);
        });
    </script>
</body>
</html>
